#!/bin/sh
yay -S zip zoom visual-studio-code-bin docker-compose postman-bin google-chrome localsend-bin
pip install aws-sam-cli 
sam --version

# expanso todoist supabase brew cisco conect 

# /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)" 
#  (echo; echo 'eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"') >> /home/botom/.bashrc  
#  (echo; echo 'eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"') >>  ~/.zshrc  